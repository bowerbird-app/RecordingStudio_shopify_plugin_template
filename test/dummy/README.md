# Dummy App

This Rails app is the Recording Studio host for the Shopify plugin demo.

Sign in at `/users/sign_in` with `admin@admin.com` / `Password`. Connect at `/shopify_plugin_demo/connect`. Pages at `/pages`. Storefront mount at `/shopify_plugin_demo/storefront/embed.js`. Named API `shopify_plugin_demo`.

Point Shopify App Home at this origin with `HOST_BASE_URL`. See the repository README and `shopify/README.md`.
