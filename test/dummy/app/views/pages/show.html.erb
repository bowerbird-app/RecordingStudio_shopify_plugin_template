<% dummy_page_nav(title: @page_recording.recordable.title, back_url: pages_path, back_label: "Pages") %>

<div class="flex w-full flex-col gap-6">
  <%= render FlatPack::PageTitle::Component.new(
    title: @page_recording.recordable.title,
    subtitle: "Recording id #{@page_recording.id}",
    variant: :h1
  ) %>
</div>
